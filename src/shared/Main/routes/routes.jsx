import MainApp from '../../Main/containers/MainApp.jsx';

import {Route, IndexRoute} from 'react-router'
import React from 'react';

const mainRoutes = (
    <Route path="/" component={MainApp}>


    </Route>
);

export default mainRoutes;
